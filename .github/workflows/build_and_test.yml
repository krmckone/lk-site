name: Build and Test

on:
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    uses: ./.github/workflows/reusable_build_and_test.yml
    secrets:
      STEAM_API_KEY: ${{ secrets.STEAM_API_KEY }}
